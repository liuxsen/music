export default class Mv {
  constructor (mv) {
    this.name = mv.name
    this.id = mv.id
    this.cover = mv.cover
    this.desc = mv.desc
    this.playCount = mv.playCount
    this.authorName = mv.authorName
    this.authorId = mv.authorId
  }
}
